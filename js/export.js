'use strict';

// Day 1 stub — export functions are implemented in Day 4 (PNG/JPG/TXT/HTML)
// and Day 5 (GIF). Day 9 adds ANSI export.

function notYetImplemented(format) {
  showToast(`${format} export arrives in a later day — stay tuned.`, 'info');
}
